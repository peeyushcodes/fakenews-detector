[project]
name = "fakenews-detector"
version = "0.1.0"
description = "Fake News OpenEnv Environment"
authors = [{ name="Peeyush Kumar" }]

dependencies = [
  "openai",
  "fastapi",
  "uvicorn",
  "openenv-core>=0.2.0"
]

[tool.openenv]
entrypoint = "server.app:app"
